% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/video_grid.R
\name{video_grid}
\alias{video_grid}
\title{Arrange Multiple Videos in a Grid}
\usage{
video_grid(
  x,
  ncol = NULL,
  nrow = NULL,
  scale = NULL,
  out_path = NULL,
  out_name = "video_grid",
  container = c("mp4", "mkv", "webm", "mov"),
  fill = "black",
  audio = c("first", "none"),
  shortest = TRUE,
  encoder = c("libx264", "libx265", "libvpx_vp9", "h264_videotoolbox"),
  quality_crf = NULL,
  preset = NULL,
  fps_out = NULL,
  overwrite = FALSE,
  flags_add_before_i = "",
  flags_add_after_i = "-pix_fmt yuv420p",
  which_ffmpeg = "ffmpeg8",
  run_cmd = TRUE
)
}
\arguments{
\item{x}{A character vector of video file paths, or a directory containing
video files. Directory entries are sorted by file name.}

\item{ncol, nrow}{Number of columns and rows. Supply either value, both values,
or neither. When neither is supplied, a near-square layout is chosen. The
grid must have room for every input.}

\item{scale}{Numeric vector `c(width, height)` giving the size of each grid
cell in pixels. When `NULL`, the largest input width and height are used.}

\item{out_path}{Output directory. Defaults to the directory of the first
input.}

\item{out_name}{Output file name, with or without an extension.}

\item{container}{Output container used when `out_name` has no extension.}

\item{fill}{FFmpeg color used for letterboxing and empty grid space.}

\item{audio}{Keep audio from the `"first"` input or discard it with
`"none"`.}

\item{shortest}{Stop when the shortest input ends. When `FALSE`, FFmpeg uses
the longest input.}

\item{encoder}{FFmpeg video encoder.}

\item{quality_crf}{Optional value for FFmpeg's `-crf` option.}

\item{preset}{Optional FFmpeg encoder preset.}

\item{fps_out}{Optional output frame rate.}

\item{overwrite}{Overwrite an existing output file? When `FALSE`, an
existing output path is made unique.}

\item{flags_add_before_i}{Additional FFmpeg flags placed before the input
arguments.}

\item{flags_add_after_i}{Additional FFmpeg flags placed after the generated
mapping and encoding arguments.}

\item{which_ffmpeg}{FFmpeg executable name or path.}

\item{run_cmd}{Run the command? When `FALSE`, only return it.}
}
\value{
The FFmpeg command as a character string, invisibly when it is run.
}
\description{
Build an FFmpeg command that places multiple videos in a regular grid. A
single row produces a side-by-side video. Inputs are scaled to fit a common
cell while preserving their aspect ratios, and any remaining space is filled
with `fill`.
}
\examples{
\dontrun{
# Two videos side by side
video_grid(
  c("left.mp4", "right.mp4"),
  nrow = 1,
  out_name = "side_by_side.mp4"
)

# Build (but do not run) a 2 x 2 grid command
video_grid(
  c("one.mp4", "two.mp4", "three.mp4", "four.mp4"),
  ncol = 2,
  scale = c(640, 360),
  run_cmd = FALSE
)
}
}
